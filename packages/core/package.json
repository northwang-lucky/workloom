{
  "name": "@workloom/core",
  "version": "0.0.0",
  "description": "workloom 核心逻辑层：任务生命周期、工作流契约、breadcrumb 组装、journal（与 runtime 无关）",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test 'test/*.test.js'"
  },
  "dependencies": {
    "yaml": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
