小北，启动工具拒绝的原因是 `prd.md` 在上次对齐确认之后又发生了变化（新增了平台目标），记录的确认哈希与当前文件内容不再匹配，旧凭证已失效。我不会绕过这个保护、也不会复用旧确认，而是按下面的顺序重新对齐：

## 处理顺序

1. **只重开受影响的区域**：其他已对齐内容（范围、目标、非目标、验收标准等）保持不变，先聚焦新增的平台目标这一块进行提问。
2. **重算完整对齐面**：新平台可能反向影响既有结论（技术选型、依赖、部署、测试矩阵），在宣布收敛前把全部区域重新过一遍，确认没有隐藏冲突。
3. **更新 `prd.md`**：把讨论结果写回 PRD，标注新增平台目标及其对既有条目的修订。
4. **重新走确认流程**：先执行 `workloom_task_align review` 生成最新对齐摘要给你过目，经你确认后再执行 `workloom_task_align confirm`（此时工具会基于当前文件计算新哈希）。
5. **重新启动**：confirm 通过后再调用 start 工具进入实现阶段。

## 第一批问题（仅针对新增平台目标）

1. 新平台目标的定位：
- A. 与现有平台并列的一等支持目标，需要完整适配与测试覆盖
- B. 实验性/渐进支持，允许功能子集，后续迭代补齐
- C. 仅需构建产物可运行，不做针对性优化

2. 新平台与现有平台的差异点中，哪些必须在本次任务内解决：
- A. 仅运行时/构建差异（如 API 兼容、打包目标）
- B. 还涉及 UI/交互差异
- C. 还涉及分发与部署链路差异

3. 测试与验收口径：
- A. 新平台纳入既有验收标准，统一一套用例
- B. 新平台单独列验收条目，允许差异化标准

## 第二批问题（重算对齐面时触发，视第一批回答提出）

4. 新增平台是否改变既有技术选型或引入新依赖（构建工具、polyfill、平台 SDK）？
5. 新平台是否影响既有非目标——原本排除的能力在新平台上是否仍然排除？
6. 排期与优先级：新平台适配是否与原范围同期交付，还是拆分里程碑？

## 初步建议

- 对问题 1 推荐 **A**：新增平台目标已进入 PRD，说明它是正式交付目标，按一等支持处理可以避免后期返工；若排期紧张再降级为 B。
- 对问题 3 推荐 **A**：统一验收口径能让 check 阶段一套用例覆盖全部平台，减少维护分支。
- 若问题 4 确认引入新依赖，建议在 PRD 的「技术选型/依赖」小节显式登记，并同步检查是否触发调研（1.2 Research）环节补充该平台的事实依据。

## 计划执行的工具动作

1. 收到你对上述问题的回答后，编辑 `prd.md`：写入新平台目标的定位、适配范围、验收口径，以及因重算对齐面而需要修订的既有条目。
2. 调用 `workloom_task_align review`，把最新的完整对齐结果呈现给你逐项核对。
3. 你确认无误后，调用 `workloom_task_align confirm`（基于当前 `prd.md` 生成新哈希，不沿用旧凭证）。
4. 最后调用 start 工具启动任务，进入 2.1 实现阶段。

请先回答第一批问题，我再根据答案决定是否需要展开第二批。
