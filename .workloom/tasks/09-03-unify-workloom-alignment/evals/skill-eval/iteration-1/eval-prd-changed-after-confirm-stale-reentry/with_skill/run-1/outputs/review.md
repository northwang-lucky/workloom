小北，`workloom_task_start` 拒绝是对的：`prd.md` 在 alignment confirm 之后发生了变更，旧的确认凭证已经失效（stale），我不会直接沿用旧凭证强行启动。按照流程，需要重新进入 Phase 1.1 对齐，处理顺序如下：

1. 先只聚焦变更区域：新增的「平台目标」只影响根节点 **环境约束**（platform），所以第一轮只围绕它重新展开，不重新翻动已收敛的其他节点。
2. 随后重新计算完整 frontier：新平台可能牵连范围、验收标准、关键决策甚至边界情况，必须全树重扫一遍，确认没有其他被波及的分支，在此之前不会宣称收敛。
3. 收敛后依次执行 `workloom_task_align` review → 你显式确认 → 用 review 返回的新 SHA-256 哈希执行 confirm（带完整 summary），拿到全新凭证后再走 1.4 start。

## 第一轮：聚焦变更区域的问题批次

围绕新增平台目标，一次问清整个 frontier：

1. 新平台与现有目标的交付关系是什么？
   - A. 必须同版本同时支持（推荐）——平台间行为一致性通常是对外承诺，分阶段上线容易留下长期兼容债。
   - B. 先支持现有平台，新平台作为后续迭代 —— 交付更快，但需要在 PRD 的 Scope and non-goals 中显式排除。
2. 新平台是否引入新的运行时/依赖约束（版本下限、缺失的能力 API、构建链差异）？
   - 我的建议：若有能力差异，采用「能力探测 + 降级」而非 `if 平台` 硬分叉，保持模块低耦合；但这取决于差异大小，需要你确认。
3. 验收标准是否需要按平台分别定义可观测的 done 标准？
   - 我的建议：把共享验收标准写一份，平台特有项单列小节，避免两份标准日后漂移。

## 后续动作计划

- 你回答后，我会把结论**增量写入**任务 `prd.md` 的对应章节与 `## Alignment Decisions`（含取舍理由、被拒绝方案），并把 `<!-- workloom:open-nodes=pending|none -->` 标记保持为 `pending`，直到全树 frontier 为空。
- 若新平台触及其他节点（例如验收、关键决策、边界情况），我会合并高度相关的问题，发起新一轮编号批次，每轮都附带我的推荐与理由。
- frontier 确认为空后：定稿 `prd.md`（标记置为 `none`）→ 运行 `workloom_task_align` `action=review`，向你展示 PRD 快照和新 SHA-256 → 你明确确认该版本 → 运行 `action=confirm`，传入 review 返回的 `expectedPrdHash` 和覆盖各节点的 summary。
- 拿到与当前 `prd.md` 匹配的新凭证后，再进入 1.4 的 start 审查。

请先回答上面第一批 3 个问题，或直接补充你对新平台目标的约束说明。
